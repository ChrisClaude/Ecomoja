import * as React from 'react';
import Head from 'next/head';

const Meta = () => {
	return (
		<Head>
			<title>Ecomoja</title>
			<link rel="icon" href="/favicon.ico" />
		</Head>
	);
};

export default Meta;
